# ccresume configuration file
# Copy this file to ~/.config/ccresume/config.toml and customize as needed

# Base command to execute when resuming or starting a session.
# Useful for custom wrappers or alternative installations.
# command = "claude"

[keybindings]
# Exit the application
quit = ["q"]

# Navigate conversation list
selectPrevious = ["up"]
selectNext = ["down"]

# Confirm selection (resume conversation)
confirm = ["enter"]

# Copy session ID to clipboard
copySessionId = ["c"]

# Scroll controls for conversation preview
scrollUp = ["k"]
scrollDown = ["j"]
scrollPageUp = ["u", "pageup"]
scrollPageDown = ["d", "pagedown"]
scrollTop = ["g"]
scrollBottom = ["G"]

# Pagination controls
pageNext = ["right"]
pagePrevious = ["left"]

# Session controls
# Start a new Claude session in the selected project directory
startNewSession = ["n"]

# Open command editor to configure Claude CLI options
openCommandEditor = ["-"]

# Toggle full message view (experimental)
toggleFullView = ["f"]